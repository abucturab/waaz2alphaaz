# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.0.0
transformers>=4.40.0
datasets>=2.18.0
evaluate>=0.4.0
accelerate>=0.27.0
peft>=0.10.0

# ── Audio ─────────────────────────────────────────────────────────────────────
torchaudio>=2.0.0
librosa>=0.10.0
pydub>=0.25.0
# whisperx requires ffmpeg installed on the system: brew install ffmpeg (macOS)
whisperx>=3.1.0

# ── Text extraction ───────────────────────────────────────────────────────────
easyocr>=1.7.0          # OCR for scanned PDFs and images
pdfplumber>=0.10.0      # text extraction from text-based PDFs
python-docx>=1.1.0      # Word (.docx) text extraction

# ── Arabic / Urdu text processing ────────────────────────────────────────────
pyarabic>=0.6.14        # diacritic stripping, Arabic text normalization

# ── Evaluation ────────────────────────────────────────────────────────────────
jiwer>=3.0.0            # WER and CER computation

# ── Dev / Notebooks ───────────────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.0.0
